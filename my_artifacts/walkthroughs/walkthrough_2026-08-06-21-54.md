# Walkthrough - Разделение Заявок на «Входящие» и «Исходящие» с Индивидуальной Матрицей Статусов

**Дата и время:** 2026-08-06 21:54  
**Статус:** Выполнено & Проверено  

## Что было сделано

1. **📥 Вкладка «Входящие заявки» (`incoming`):**
   - Отображает заявки, поступившие в адрес нашей организации (`target_company_id === currentCompanyId`).
   - Иерархические статусы: *Все*, *Ожидают вашего ответа (`pending`)*, *Принятые вами (`approved`)*, *Отклоненные вами (`rejected`)*.
   - Исполнение действий: `[ Принять в сеть ]` (авто-создание контрагентов) и `[ Отклонить ]`.

2. **📤 Вкладка «Исходящие заявки» (`outgoing`):**
   - Отображает заявки, отправленные нашей организацией другим компаниям (`requester_company_id === currentCompanyId`).
   - Иерархические статусы: *Все*, *На рассмотрении у партнера (`pending`)*, *Подтверждены партнером (`approved`)*, *Отозваны вами (`recalled`)*, *Отклонены партнером (`rejected`)*.
   - Исполнение действий: `[ Отозвать заявку ]` и `[ Повторить запрос ]`.

---

## Результаты сборки
- **Сборка `npm run build`:** `✓ Compiled successfully (22/22 static pages generated)`.
