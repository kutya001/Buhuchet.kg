# Walkthrough - Полная Реструктуризация Процессов Регистрации, Подачи Заявок и Управления Сотрудниками

**Дата и время:** 2026-08-06 21:23  
**Статус:** Выполнено & Проверено  

## Что было сделано

1. **🗄️ База Данных & Безопасность RLS (`DATABASE.md`):**
   - Добавлены поля статуса заявки `status` (`'pending'`, `'active'`, `'rejected'`, `'blocked'`), `position` и `created_at` в схему `company_users`.
   - Зафиксированы RLS политики публичного чтения списка организаций `{ id, name }` для Onboarding и блокировка редактирования чужих профилей.

2. **🚀 Onboarding & Выбор Компании (`app/onboarding/`):**
   - Добавлен серверный экшен `getPublicCompaniesListAction()` для формирования публичного списка названий организаций.
   - Реализована функция подачи заявки сотрудником `submitJoinCompanyRequestAction(companyId, position)` со статусом `'pending'`.

3. **⏳ Экран Ожидания (`/dashboard/pending`) & Middleware:**
   - Разработан экран ожидания `app/dashboard/pending/page.tsx` с уведомлением об отправке заявки владельцу и кнопкой «Отозвать заявку / Выбрать другую компанию».
   - В `middleware.ts` подключена автоматическая защита маршрутов: сотрудники со статусом `'pending'` или `'rejected'` перенаправляются на `/dashboard/pending`.

4. **👥 Управление Заявками и Штатом (`/dashboard/employees/`):**
   - **Серверные Экшены (`actions.ts`):** ❌ Ручное создание учётных записей владельцем и сброс чужих паролей полностью удалены. ✅ Реализованы экшены `getPendingRequestsAction`, `approveEmployeeRequestAction`, `rejectEmployeeRequestAction`, `updateEmployeeRoleAndPositionAction` (редактирует только 2 поля: Роль и Должность) и `removeEmployeeAction`.
   - **Интерфейс (`page.tsx`):** Добавлен блок **«Заявки на вступление»** с утверждением/отклонением поступивших запросов и блок **«Штат сотрудников»** с сохранением быстрых коммуникаций (🟢 WhatsApp, 🔵 Telegram, 📞 Звонок) и модальным окном смены Роли/Должности.

---

## Результаты сборки
- **Сборка `npm run build`:** `✓ Compiled successfully (22/22 static pages generated)`.
