# Walkthrough - Рефакторинг и Расширение Гранулярной Ролевой Модели (RBAC & Tab-level Permissions)

**Дата и время:** 2026-07-31 19:57  
**Статус:** Успешно реализовано и проверено  

## Что было реализовано

### 1. 🔑 Расширение типов и справочника разрешений (`types/database.types.ts` & `lib/auth/permissions.ts`)
- Добавлен модуль `dashboard` в единую матрицу прав.
- Внесены гранулярные права контроля вкладок:
  - `tab_counterparties`, `tab_partnerships`, `tab_catalog` — модуль Организации.
  - `tab_profile`, `tab_legal_docs` — модуль Моя Организация.
  - `tab_my_profile`, `tab_employees`, `tab_roles` — модуль Сотрудники.
- Внесены экшены контроля уставных сканов: `upload_legal_doc`, `add_legal_doc`, `edit_legal_doc`, `delete_legal_doc`.
- Внесены экшены управления ролями и профилями: `edit_my_profile`, `create_role`, `edit_role`, `delete_role`.
- Внесена фильтрация по статусам первички: `view_all_statuses`, `view_draft_only`, `view_sent_only`, `view_accepted_only`.

### 2. 🛡️ Защита аккаунта Владельца организации (`app/dashboard/employees/actions.ts` & `page.tsx`)
- Внедрён серверный и клиентский запрет на изъятие ролей, сброс паролей или изменение данных аккаунта Владельца (`role === 'owner'`) любыми другими сотрудниками (даже при наличии права `edit_employee`).

### 3. 🖥️ Динамический контроль сайдбара и вкладок (`DashboardShell.tsx`, `counterparties/page.tsx`, `company/page.tsx`, `employees/page.tsx`, `documents/page.tsx`)
- Меню сайдбара скрывает пункт «Главная» при отсутствии разрешения `dashboard:view`.
- Модули «Организации», «Моя Организация» и «Сотрудники» динамически отображают только разрешённые пользователю вкладки.
- Модуль «Документы» фильтрует строки первички строго в соответствии с доступными статусами.

---

## Результаты сборки
- **Сборка `npm run build`:** `✓ Compiled successfully (21/21 static pages)`.
- **Типизация TypeScript:** 100% валидна без ошибок.
