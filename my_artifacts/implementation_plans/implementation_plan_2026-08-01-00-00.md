# Implementation Plan - Перенос Времени, Даты и Тем в Нижнюю Шторку и Удаление 3 Точек из Topbar

**Дата создания:** 2026-08-01 00:00  
**Статус:** Ожидает утверждения пользователя  

---

## 1. 🧹 Удаление «3 Точек» из Верхней Панели (`components/ui/FloatingTopbar.tsx`)
- Полное вырезание кнопки `MoreVertical` и связанного `DropdownMenu` из `FloatingTopbar.tsx`.
- Верхняя панель на смартфонах остается предельно чистой: кнопка-гамбургер `Menu` и лупа поиска `Search`.

---

## 2. 📱 Встраивание Часов, Даты и Выбора Тем в Нижнее Выездное Меню (`components/dashboard/DashboardShell.tsx`)
- Перенос динамического виджета времени и даты КР в шапку мобильной выездной карточки (`Bottom Sheet Drawer`).
- Встраивание чистого переключателя 3 тем оформления (`Moon` Тёмная, `Sun` Светлая, `Coffee` Тёплая).
- Оснащение гидратационной защитой (`mounted` стейт и `suppressHydrationWarning`).

---

## 3. План Валидации
1. Проверка типов и сборка `npm run build`.
2. Фиксация обновлений в Git (`git push origin main`).
