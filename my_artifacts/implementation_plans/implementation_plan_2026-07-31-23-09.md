# Implementation Plan - Выездной Мобильный Сайдбар (Drawer), Гамбургер в Topbar и Устранение Горизонтального Сжатия

**Дата создания:** 2026-07-31 23:09  
**Статус:** Ожидает утверждения пользователя  

---

## 1. Сайдбар и Мобильный Drawer (`components/super-admin/SuperAdminSidebar.tsx`)
- Десктопный `aside` адаптируется через `hidden md:flex`.
- Мобильный выездной занавес (Drawer/Overlay) отображается при `isOpenMobile === true`.
- Кнопка закрытия `X` и автоматическое закрытие шторки при вызове `onTabChange`.

---

## 2. Кнопка Гамбургер в Верхней Панели (`components/ui/FloatingTopbar.tsx`)
- Внедрение мобильной кнопки-гамбургер (`Menu`) слева в `FloatingTopbar` (`md:hidden`), вызывающей `onOpenMobileMenu`.

---

## 3. Очистка Контейнера и Таблиц (`app/super-admin/page.tsx`)
- Использование `min-w-0 w-full overflow-hidden` на контейнере рабочей области.
- Предотвращение сплющивания таблиц и выталкивания кнопок за пределы экрана.

---

## 4. План Валидации
1. Проверка типов и сборка `npm run build`.
2. Фиксация обновлений в Git (`git push origin main`).
