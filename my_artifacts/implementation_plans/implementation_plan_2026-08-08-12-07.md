# План исправления редиректа и отображения модерации организации после онбординга

## Описание задачи
После регистрации новой организации пользователь (владелец компании) перенаправляется на страницу `/dashboard/pending`, где отображается некорректный статичный текст о присоединении сотрудника к чужой организации (*«Ваш запрос на присоединение к организации находится у Руководителя»*).

## Proposed Changes

### 1. Onboarding Module
#### [MODIFY] [app/(auth)/onboarding/page.tsx](file:///d:/Рабочий%20стол/Code%20Projects/Buhuchet.kg/app/(auth)/onboarding/page.tsx)
- Изменить маршрут редиректа после успешного сохранения организации в `createCompanyOnboardingAction`:
  - Вместо `router.push('/dashboard/pending')` выполнять `router.push('/dashboard')`.
  - Владелец компании будет переходить прямо на дашборд, где отображается баннер модерации.

### 2. Pending Application Page
#### [MODIFY] [app/dashboard/pending/page.tsx](file:///d:/Рабочий%20стол/Code%20Projects/Buhuchet.kg/app/dashboard/pending/page.tsx)
- Добавить получение `status` компании и `role` пользователя в `checkStatus()`.
- Редиректить пользователей с ролью `owner` / `accountant` на `/dashboard`.
- Адаптировать интерфейс для двух режимов:
  1. **Модерация организации (Руководитель / Создатель)**: выводить информацию о проверке суперадминистратором платформы Buhuchet.kg.
  2. **Присоединение сотрудника**: выводить информацию об ожидании подтверждения владельцем компании.

## Verification Plan
1. Проверить регистрацию новой организации: редирект происходит на `/dashboard` с отображением баннера модерации.
2. Проверить прямой доступ к `/dashboard/pending`: владелец автоматический редиректится на `/dashboard`, либо видит корректный текст модерации организации.
