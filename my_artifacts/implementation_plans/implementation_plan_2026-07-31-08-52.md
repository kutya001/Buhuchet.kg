# План Имплементации: Устранение Багов Тем и Полный Перевод Всех Элементов Системы на Динамические Токены

В данном документе изложен детальный план устранения багов непереключенных элементов, шрифтов и карточек на страницах авторизации, лендинге, главном дашборде и суперадминке для идеальной работы 3-х тем (🌙 Тёмная, ☀️ Светлая, ☕ Тёплая).

---

## 🛠️ Перечень Предлагаемых Изменений по Зафиксированным Багам

### 1. [Страницы Входа, Регистрации и Онбординга (`app/(auth)/...`)]
- **Файлы:**
  - `app/(auth)/layout.tsx`
  - `app/(auth)/login/page.tsx`
  - `app/(auth)/register/page.tsx`
  - `app/(auth)/onboarding/page.tsx`
- **Изменения:**
  - Добавить переключатель тем `<ThemeToggle />` в верхний угол экранов входа/регистрации.
  - Заменить жесткие фоны `bg-slate-950` и `bg-slate-900` ➔ `bg-background` и `bg-card border-border`.
  - Заменить шрифты `text-white`, `text-slate-100` ➔ `text-foreground`, а плейсхолдеры и описание ➔ `text-muted-foreground`.

### 2. [Лендинг (`app/page.tsx`)]
- **Изменения:**
  - **Главный заголовок:** "Электронный документооборот и облачный архив первички" — настроить адаптивный градиент, считываемый 100% на светлом, бежевом и тёмном фоне.
  - **Кнопки:**
    - Кнопка "Старт" / "Подключить" в шапке ➔ адаптивный дизайн.
    - Кнопка "Зарегистрировать Организацию" и "Войти в личный кабинет" ➔ `bg-card border-border text-foreground`.
  - **Значок платформы:** Динамический цвет иконки и текста логотипа `text-foreground` / `text-primary`.
  - **Футер (Самый нижний элемент):** Заменить `bg-slate-950` ➔ `bg-card border-t border-border text-foreground`.

### 3. [Главный Дашборд (`app/dashboard/page.tsx`)]
- **Изменения:**
  - Перевести все карточки статистики, график, быструю навигацию и списки с `bg-slate-900`, `text-white`, `text-slate-400` ➔ `bg-card border-border`, `text-foreground`, `text-muted-foreground`.

### 4. [Реестры Первички, Контрагентов, Файлов, Сотрудников, Профиля Компании и Суперадминки]
- **Файлы:**
  - `app/dashboard/documents/page.tsx`, `[id]/page.tsx`, `new/page.tsx`
  - `app/dashboard/counterparties/page.tsx`
  - `app/dashboard/files/page.tsx`
  - `app/dashboard/employees/page.tsx`
  - `app/dashboard/company/page.tsx`
  - `app/super-admin/page.tsx`
- **Изменения:**
  - Рефакторинг оставшихся `text-white` и `bg-slate-950` ➔ `text-foreground` и `bg-card`.

---

## 📊 План Верификации

1. **Проверка `npx tsc --noEmit`:** 0 ошибок типов.
2. **Проверка продакшн сборки (`npm run build`):** 100% сборка всех страниц.
3. **Визуальная валидация (QA):**
   - На страницах Входа, Лендинга, Дашборда и Суперадминки переключение тем происходят мгновенно и без нечитаемых элементов.
