# Implementation Plan - Адаптация Админки, Мобильное Меню «3 точки», Mobile FAB и Фикс Ширины

**Дата создания:** 2026-07-31 22:57  
**Статус:** Ожидает утверждения пользователя  

---

## 1. Сайдбар Суперадминки (`components/super-admin/SuperAdminSidebar.tsx`)
- Перенос брендинга «Панель Суперадминистратора» и бэйджа `ADMIN` в шапку сайдбара.
- Удаление кнопки «В Дашборд».
- Размещение кнопки **«Выйти из системы»** (`LogOut`) в самом низу панели.

---

## 2. Мобильное Меню «3 точки» в `FloatingTopbar.tsx`
- Интеграция `DropdownMenu` с триггером `MoreVertical` для смартфонов (`< 768px`).
- Внутри выпадающего меню: Время КР, Дата, Переключатель темы `ThemeToggle`, кнопка «Выйти».

---

## 3. Мобильная Плавающая Кнопка (`components/ui/MobileFAB.tsx`)
- Создание `MobileFAB.tsx`: круглая кнопка под правый палец `bottom-6 right-5` (скрыта на ПК, видна на мобилках).

---

## 4. Исправление Ширины Контейнера Админки (`app/super-admin/page.tsx`)
- Применение `isFullWidth` к главному блоку `main` с переключением между `max-w-7xl mx-auto` и `w-full max-w-none px-6`.
- Удаление любых дублирующих верхних баров в `app/super-admin/page.tsx`.

---

## 5. План Валидации
1. Проверка типов и сборка `npm run build`.
2. Фиксация обновлений в Git (`git push origin main`).
