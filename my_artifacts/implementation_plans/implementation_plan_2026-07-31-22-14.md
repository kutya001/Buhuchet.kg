# Implementation Plan - Оптимизация Границ Таблиц, Левый Сайдбар и Быстродействие Панели Суперадминистратора

**Дата создания:** 2026-07-31 22:14  
**Статус:** Ожидает утверждения пользователя  

---

## 1. Обновление базового компонента Таблицы (`components/ui/table.tsx`)
- Добавление вертикальных границ столбцов `border-r border-border/60` для `TableHead` и `TableCell`.
- Выделение чередующихся строк (`[&_tr:nth-child(even)]:bg-muted/15`) и единых токенов темы.

---

## 2. Создание Левого Вертикального Сайдбара (`components/super-admin/SuperAdminSidebar.tsx`)
- Перенос навигации из верхней горизонтальной плашки в левый сайдбар.
- Элементы навигации: Организации, Пользователи, Файлы R2, Документы, Telegram Бот, Справочники, Инспектор БД.
- Кнопка быстрый возврат в рабочий кабинет (`/dashboard`).

---

## 3. Модернизация Панели Суперадминистратора (`app/super-admin/page.tsx`)
- Интеграция `SuperAdminSidebar` с клиентским переключением за **0 мс** через `useTransition`.
- Полная синхронизация цветовой темы (CSS-токены `background`, `card`, `border`, `foreground`).
- Бесшовная интеграция всех имеющихся модулей (включая Telegram Бот, Файлы R2 и Инспектор БД).

---

## 4. План Валидации
1. Проверка типов и сборка проекта `npm run build`.
2. Фиксация обновлений в Git (`git push origin main`).
